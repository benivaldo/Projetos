<?php
$title = 'Adicionar novo ICMS';
$this->headTitle($title);
?>
<h4><?php echo $this->escapeHtml($title); ?></h4>

<?php
$form = $this->form;
$id = '';
$form->setAttribute('id', 'formIcmsAdd');
$form->setAttribute('class', 'form-horizontal form-add');
$form->setAttribute('action', 'cadimposto/cadicms/add');
$form->setAttribute('data-modulo', 'cadimposto');    //variavel de controle com o nome do modulo
$form->setAttribute('data-ctrl', 'cadicms');      //variavel de controle com o nome do controle
$form->setAttribute('data-titulo', 'ICMS');         //variavel de controle com o titulo da tab
$form->setAttribute('data-div', $this->escapeHtml($this->div));         //variavel de controle com o titulo do nome da div abas
$form->setAttribute('data-order_by', 'icms_id');    //variavel de controle com o nome da chave primaria
$form->setAttribute('data-id', $id);    //variavel de controle com o id a ser consultado
$form->prepare();

?>
<div class="container-fluid">
	  <?php echo $this->form()->openTag($form)  ?>
          <div role="tabpanel">
            <ul class="nav nav-tabs" role="tablist">
              <li class="active"><a href="#abaicms1" data-toggle="tab" role="tab">Principal</a></li>
              <li><a href="#abaicms2" data-toggle="tab" role="tab"></a></li>
              <li><a href="#abicms3" data-toggle="tab" role="tab"></a></li>
            </ul>
            <div id="tabContent1" class="tab-content">
              <div class="tab-pane fade in active cadastro" id="abaicms1">
                <?php echo $this->partial('cadimposto/cadicms/partialPrincipal.phtml', array('form' => $form, 'tipo' => 'add')); ?>
              </div>                
              <div class="tab-pane fade cadastro" id="abaicms2">
                
              </div>
              <div class="tab-pane fade cadastro" id="abaicms3">
                <p>Dropdown content#1</p>
              </div>
            </div>	     
          </div>
      <?php echo $this->partial('cadimposto/partials/botoes/partialBotao.phtml', array('classe' => 'disabled')); ?>
      <?php echo $this->form()->closeTag($form) ?> 
      <div id="div_popup_icms" class="popup"></div>
    </div>

